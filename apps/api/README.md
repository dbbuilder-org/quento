# Quento API

AI-Powered Business Growth Platform API

## Setup

```bash
poetry install
poetry run uvicorn app.main:app --reload
```

## Environment Variables

See `.env.example` for required configuration.
